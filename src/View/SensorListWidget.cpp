#include"SensorListWidget.h"

namespace View{

SensorListWidget::SensorListWidget(QWidget* parent)
    : QWidget(parent)
{

}

void SensorListWidget::show(){
    // layout = new QGridLayout(this);
    // for(int i = 0; i < lookup.size(); i++){
    //     layout->addWidget(lookup[i].getWidget(), i, 0);
    //     layout->addWidget(lookup[i].getViewButton(), i, 1);
    //     layout->addWidget(lookup[i].getEditButton(), i, 2);
    //     layout->addWidget(lookup[i].getDeleteButton(), i, 3);
    // }
    // setLayout(layout);
}

}
